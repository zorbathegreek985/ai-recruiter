# Copy this file to secrets.toml and fill in your key (for local testing)
# On Streamlit Cloud, go to your app settings > Secrets and paste the content below (without the .example)

GOOGLE_API_KEY = "your_gemini_api_key_here"

# Optional future secrets
# OTHER_API_KEY = "..."